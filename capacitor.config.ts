import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ET_Programacion',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
